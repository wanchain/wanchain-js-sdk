'use strict'

// common
let compileContract = require('./utils/scTool').compileContract;
let deployContract  = require('./utils/scTool').deployContract;

module.exports = {
  compileContract,
  deployContract
};
